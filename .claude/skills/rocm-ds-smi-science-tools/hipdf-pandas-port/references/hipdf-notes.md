Core hipDF notes:
- GPU DataFrames with pandas-like API
- Python and C++ APIs
- Targets tabular ETL, aggregation, joins, filters
- Supports cudf.pandas-style acceleration path
- Official support is narrower than generic ROCm support
- Audit unsupported features before porting
